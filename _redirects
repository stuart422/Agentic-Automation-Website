# Netlify _redirects — enforce canonical domain and HTTPS

# www → non-www (301 permanent)
https://www.agentic-go.com/*   https://agentic-go.com/:splat   301!

# HTTP → HTTPS is handled automatically by Netlify
# No explicit rule needed — Netlify forces HTTPS by default when enabled in site settings
